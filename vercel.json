{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @workspace/lotto-app run build",
  "outputDirectory": "artifacts/lotto-app/dist/public",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ]
}
